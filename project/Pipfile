[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
alembic = ">=1.13.0,<2" # Remove if alembic migrations will not be used.
equipment = ">=1.0.0a1,<2"
fastapi = { extras = ["standard"], version = ">=0.100.0,<1" } # Remove if web.py will not be used.
uvicorn = { extras = ["standard"], version = ">=0.30,<1" } # Remove if web.py will not be used.
#psycopg2-binary = ">=2.9,<3" Enable to use PostgreSQL as a database connection
#mysqlclient = ">=2.2,<3" Enable to use MySQL as a database connection
#mysql-connector-python = ">=9.1,<10" Enable to use MYSQL as a database connection.

[dev-packages]
pylint = ">=3,<4"
autopep8 = ">=2,<3"
flake8 = ">=7.0.0,<8"
coverage = ">=7,<8" # coverage run -m unittest
runtype = ">=0.5,<1"
faker = ">=33"

[requires]
python_version = "3.13"
